// Firebase Firestore Security Rules
// Copy these rules to your Firebase console: https://console.firebase.google.com/
// Navigate to Firestore Database > Rules and paste these rules

// OPTION 1: Public access (for testing only)
// This allows anyone to read and write to your database
// DO NOT use in production!
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// OPTION 2: Authenticated access (for production)
// Uncomment this section and comment out OPTION 1 when you implement authentication
/*
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to habits collection
    match /habits/{habitId} {
      // Allow read if authenticated
      allow read: if request.auth != null;
      
      // Allow write if authenticated
      allow create, update, delete: if request.auth != null;
      
      // Allow read and write access to habit logs subcollection
      match /logs/{logId} {
        allow read, write: if request.auth != null;
      }
    }
    
    // Allow read and write access to habit_logs collection
    match /habit_logs/{logId} {
      allow read, write: if request.auth != null;
    }
    
    // Allow read and write access to test_collection (for connection testing)
    match /test_collection/{docId} {
      allow read, write: if true;
    }
  }
}
*/ 